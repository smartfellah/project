import styles from "./header.module.css";

import React, { FC } from "react";

const Header: FC = () => {
  return (
    <div className={styles.Header}>
      <div></div>
    </div>
  );
};

export { Header };
